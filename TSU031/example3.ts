namespace example3 {
  function myFunction(x: number) {
    if (x === 1) {
      return "one";
    } else if (x === 2) {
      return [1, 2, 3];
    } else if (x === 3) {
      return true;
    }
  }
}
